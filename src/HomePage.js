import React, { useState } from 'react';
import { fetchAIResponseWithTiming } from './Api';

export default function HomePage() {
  // copy over your existing state and handleSubmit, but call the timed API helper
  // render exactly what you have now
}
